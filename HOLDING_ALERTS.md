# 보유관리 알림 (통합지시 3) — 사용·입력·비공개 안내

실제 체결(positions.csv) 기준으로 **추가매수 · 손절 · 20일(L20) 청산**을 라이브 관측가로 판정해
텔레그램으로 알린다. 계산 규칙(½N 추가·2N 손절·½N 인상·L20)과 발송(Notifier)은 기존 것을 재사용.
자동 주문은 없다. 알림은 "지금 조건에 닿았다"는 통지일 뿐, 매수·매도 완료로 처리하지 않는다.

## 무엇을 알리나
- **추가매수 도달**: 관측가 ≥ (마지막 실제 체결가 + ½N), 유닛 4개 미만일 때만.
- **손절**: 관측가 ≤ 해당 유닛 손절선(체결가 − 2N, 유닛 추가마다 기존 유닛 ½N 인상). 유닛별로 알림.
- **L20 청산**: 관측가 ≤ (직전 20일 저가 − 1틱) → 잔여 보유 전량 청산.
- 한 관측에서 여러 유닛 손절 → **한 메시지로 묶음**. L20까지 겹치면 **잔여 전량 청산 한 건**으로 합쳐 표시(중복 매도 지시처럼 안 보이게).

## 체결 입력 (positions.csv)
`live/positions.csv` 에 **한 줄 = 한 유닛**으로 입력한다(템플릿: `positions.sample.csv`).

| 열 | 설명 |
|---|---|
| symbol | 종목(예: AAPL) |
| campaign_id | 한 번의 진입~전량청산 묶음 이름(예: AAPL-2026-09). 같은 캠페인 유닛은 같은 값 |
| unit_no | 유닛 번호 1~4 |
| fill_date | 체결일 YYYY-MM-DD |
| fill_time / fill_tz | **오늘 체결한 유닛은 시각(HH:MM:SS)·시간대 필수**(예: America/New_York). 과거일은 생략 가능 |
| fill_price / shares | 실제 체결가 / 수량 |
| trade_n | 캠페인 N. **필수**. 비우면 그 종목은 'N 입력 필요'로 알림 안 함(추정 안 함) |
| exit_date / exit_price | 그 유닛을 **매도하면 그 유닛 행에 채움**(새 행 아님). 이후 그 유닛은 알림에서 제외 |
| note | 메모(자유) |

수정/변경:
- **추가 체결**: 새 unit_no 행 추가 → 손절선·추가매수선이 새 체결 기준으로 바뀌고, 그 체결시각 이후 시세부터 적용. 바뀐 알림만 다시 울린다(무관한 알림 반복 없음).
- **매도**: 그 유닛 행에 exit_date·exit_price 입력 → 그 유닛은 빠지고, 손절 인상 이력은 보존.
- **행 순서**는 아무렇게나 둬도 동일하게 처리된다.

## 왜 '체결 전 가격'으로 알림이 안 뜨나
각 알림은 그 기준선을 정한 **체결시각 이후 시세만** 사용한다. 그래서 당일 매수 전에 지나간 가격으로는 손절/청산 알림이 뜨지 않는다. 그래서 **오늘 체결은 시각·시간대가 필수**(없으면 '체결시각 입력 필요'로 표시하고 그 알림은 보류). 관측을 시작한 시점에 이미 선을 넘어 있으면 '첫 관측부터 조건 충족'으로 구분 표시한다(돌파 순간을 포착한 것으로 쓰지 않음).

## 실행
```
# 기준가표(levels) 먼저 만든 뒤, 보유관리까지 포함해 스트림 실행
python3 live/turtle_live_stream.py --levels live/out/levels.csv --positions live/positions.csv --minutes 30
```
- 보유 종목은 후보 풀과 무관하게 자동으로 수신 대상에 포함된다.
- 텔레그램 미설정이면 발송만 꺼지고 감시·기록은 정상.
- 테스트 발송: `python3 live/holding_alerts_selftest.py` (자격 있으면 **[테스트]** 접두로 실제 발송 → 실사용 알림과 구분).

## 보유정보 비공개 (중요)
- **로컬 기본**: `positions.csv`·`account.json` 은 네 맥에만. `.gitignore` 로 공개 저장소에서 제외됨. 알림 기록/요약은 `live/out_yahoo/`(역시 .gitignore) 에만 남고 공개 산출물엔 안 들어감.
- **클라우드(GitHub 등)에서 돌릴 때**: 공개 저장소에 보유정보를 넣지 않는다. 대신 체결 CSV 내용을 base64로 만들어 **GitHub Secret `POSITIONS_CSV_B64`** 에 저장 → 실행 중 임시파일로만 만들었다가 **종료 시 즉시 삭제**한다(로그·캐시·산출물에 원본 안 남김). 별도 private 저장소는 이번에 만들지 않음.
  - CSV → Secret 값 만들기(맥): `base64 -i live/positions.csv | pbcopy` → GitHub Secret 에 붙여넣기.
- **남은 항목(다음 단계)**: 클라우드 실제 운영에서 '발송 이력(sent_history) 영속화'는 운영 연결 단계에서 처리(현재는 로컬 파일 기준).

## 한계
- 이번 작업은 로직 + 기존 스트림 연결 + 가상체결 검증까지. **스케줄·장 전체 무중단 운영은 포함하지 않음**.
- N 누락 시 추정하지 않는다(‘N 입력 필요’). levels 의 assumed N 값은 알림으로 넘어가지 않는다.
